# 🧠 Bi IDE - نظام التدريب الشامل للذكاء الاصطناعي

## نظرة عامة

هذا النظام يقوم بتدريب الذكاء الاصطناعي في Bi IDE على:

- ✅ **فهم الكود البرمجي** - تحليل وشرح الكود
- ✅ **إكمال الكود** - اقتراح الكود المناسب
- ✅ **اكتشاف الأخطاء** - التعرف على الأخطاء البرمجية
- ✅ **إصلاح الأخطاء** - تقديم حلول للأخطاء
- ✅ **أنماط التصميم** - تطبيق Design Patterns
- ✅ **أفضل الممارسات** - كتابة كود نظيف

## المتطلبات

- Python 3.11 أو أحدث
- pip (مدير الحزم)

## التثبيت السريع

```bash
# الدخول لمجلد التدريب
cd training

# تثبيت المتطلبات الأساسية
pip install tqdm colorama rich

# تثبيت جميع المتطلبات (اختياري)
pip install -r requirements.txt
```

## التشغيل

### الطريقة 1: ملف التشغيل (موصى به)

```bash
# على Windows
run_training.bat
```

### الطريقة 2: سطر الأوامر

```bash
# تجهيز البيانات فقط (سريع)
python train_ai.py --mode prepare

# تدريب كامل (يتطلب مكتبات ML)
python train_ai.py --mode full

# مع تفاصيل إضافية
python train_ai.py --mode prepare --verbose
```

### الطريقة 3: التدريب المتقدم

```bash
# تدريب متقدم على إصلاح الأخطاء
python advanced_training.py
```

## الملفات المُنشأة

بعد التدريب، ستجد هذه الملفات:

```
training/
├── output/
│   ├── training_data.json          # بيانات التدريب الكاملة
│   ├── error_detection_training.json
│   ├── error_fixing_training.json
│   ├── code_review_training.json
│   └── combined_training.json
│
models/
├── knowledge-base.json             # قاعدة المعرفة للتطبيق
│
data/
├── learned-knowledge.json          # المعرفة المتعلمة
```

## هيكل البيانات

### بيانات إصلاح الأخطاء

```json
{
    "instruction": "أصلح الخطأ في هذا الكود (javascript):",
    "input": "const arr = [1, 2, 3",
    "output": "const arr = [1, 2, 3];\n\n📝 الشرح: القوس المربع غير مغلق",
    "metadata": {
        "language": "javascript",
        "category": "syntax_errors",
        "type": "error_fixing"
    }
}
```

### بيانات اكتشاف الأخطاء

```json
{
    "instruction": "هل يوجد خطأ في هذا الكود (javascript)?",
    "input": "console.log(userName);",
    "output": "نعم، يوجد خطأ. يجب تعريف المتغير قبل استخدامه",
    "metadata": {
        "language": "javascript",
        "category": "reference_errors"
    }
}
```

## اللغات المدعومة

- ✅ JavaScript / TypeScript
- ✅ Python
- ✅ React / Vue
- ✅ HTML / CSS
- ✅ SQL
- ✅ وغيرها...

## أنواع الأخطاء المغطاة

### JavaScript

| النوع | الوصف |
|------|-------|
| SyntaxError | أخطاء بناء الجملة |
| ReferenceError | متغيرات غير معرفة |
| TypeError | أنواع بيانات خاطئة |
| Async Errors | أخطاء البرمجة غير المتزامنة |
| Common Mistakes | الأخطاء الشائعة |

### Python

| النوع | الوصف |
|------|-------|
| IndentationError | مسافات بادئة خاطئة |
| TypeError | أنواع غير متوافقة |
| Mutable Defaults | قيم افتراضية قابلة للتغيير |
| Late Binding | مشاكل Closures |

### React

| النوع | الوصف |
|------|-------|
| Invalid Hook | استدعاء hook خاطئ |
| Infinite Loop | حلقات لا نهائية |
| Missing Key | مفاتيح القوائم |

## الاستخدام مع Bi IDE

بعد التدريب، سيستخدم Bi IDE البيانات تلقائياً:

1. **شغّل التدريب**: `python train_ai.py`
2. **شغّل التطبيق**: `npm start`
3. **النظام سيستخدم المعرفة الجديدة**

## توسيع البيانات

### إضافة أخطاء جديدة

عدّل ملف `advanced_training.py`:

```python
ERROR_DATABASE = {
    'javascript': {
        'your_category': [
            {
                'pattern': r'your regex pattern',
                'causes': ['السبب 1', 'السبب 2'],
                'examples': [
                    {
                        'error': 'الكود الخاطئ',
                        'fix': 'الكود الصحيح',
                        'explanation': 'الشرح'
                    }
                ]
            }
        ]
    }
}
```

### إضافة أفضل ممارسات

```python
BEST_PRACTICES = {
    'your_language': [
        {
            'category': 'الفئة',
            'practices': [
                'الممارسة 1',
                'الممارسة 2'
            ]
        }
    ]
}
```

## الأوامر المتقدمة

```bash
# تدريب مع تحديد ملف الإخراج
python train_ai.py --output custom_output.json

# عرض المساعدة
python train_ai.py --help
```

## استكشاف الأخطاء

### خطأ: Python not found

```bash
# تأكد من تثبيت Python
python --version

# أو استخدم python3
python3 train_ai.py
```

### خطأ: Module not found

```bash
# تثبيت المتطلبات
pip install -r requirements.txt
```

### خطأ في الترميز

```bash
# تشغيل مع ترميز UTF-8
chcp 65001
python train_ai.py
```

## المساهمة

لإضافة المزيد من بيانات التدريب:

1. أضف أمثلة في `data/` 
2. حدّث `ERROR_DATABASE` في `advanced_training.py`
3. شغّل التدريب مجدداً

---

📧 **للأسئلة والدعم**: افتح issue في المشروع
