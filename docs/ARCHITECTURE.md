# BI-IDE Architecture Document
# وثيقة بنية نظام BI-IDE

---

## 1. Overview / نظرة عامة

### 1.1 Project Vision / رؤية المشروع

BI-IDE هو نظام ذكاء اصطناعي متكامل يجمع بين:
- **بيئة تطوير متكاملة (IDE)** مع مساعد AI ذكي
- **نظام تخطيط موارد المؤسسات (ERP)** متكامل
- **مجلس حكماء AI** متكون من 16 شخصية ذكية
- **نظام هرمي متعدد الطبقات** للذكاء الاصطناعي (15 طبقة)

### 1.2 Dual-System Architecture / البنية الثنائية

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           BI-IDE SYSTEM ARCHITECTURE                        │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ┌─────────────────────────────┐        ┌─────────────────────────────┐    │
│  │    Windows Workstation      │◄──────►│   Ubuntu RTX 4090 Server    │    │
│  │    (Frontend + API)         │  LAN   │   (AI Training + Inference) │    │
│  │    192.168.68.x:8000        │        │   192.168.68.111:9090       │    │
│  └─────────────────────────────┘        └─────────────────────────────┘    │
│           │                                        │                        │
│           ▼                                        ▼                        │
│  ┌─────────────────────────────┐        ┌─────────────────────────────┐    │
│  │  - React Frontend           │        │  - Training Server          │    │
│  │  - FastAPI Backend          │        │  - Inference Server         │    │
│  │  - IDE Service              │        │  - 15 Transformer Models    │    │
│  │  - ERP Service              │        │  - 62,000+ Epochs Trained   │    │
│  │  - Smart Council            │        │  - CUDA Acceleration        │    │
│  └─────────────────────────────┘        └─────────────────────────────┘    │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 2. System Components / مكونات النظام

### 2.1 Frontend Layer / طبقة الواجهة الأمامية

**Technology Stack:**
- React 18+ with TypeScript
- Tailwind CSS for styling
- Monaco Editor for code editing
- React Router for navigation

**Pages & Components:**
```
ui/src/
├── pages/
│   ├── Dashboard.tsx      # لوحة التحكم الرئيسية
│   ├── IDE.tsx           # بيئة التطوير المتكاملة
│   ├── ERP.tsx           # نظام تخطيط الموارد
│   ├── Council.tsx       # مجلس الحكماء (16 حكيم)
│   ├── Training.tsx      # لوحة التدريب والموديلات
│   ├── Community.tsx     # المجتمع والمشاركة
│   ├── MetaControl.tsx   # التحكم بالنظام
│   └── Settings.tsx      # الإعدادات
├── components/
│   ├── Layout.tsx        # التخطيط الرئيسي
│   ├── Header.tsx        # رأس الصفحة
│   ├── Sidebar.tsx       # القائمة الجانبية
│   └── ...
└── App.tsx               # نقطة الدخول
```

### 2.2 API Layer / طبقة الـ API

**Technology Stack:**
- FastAPI (Python 3.10+)
- Uvicorn ASGI server
- Pydantic for data validation
- CORS middleware for cross-origin requests

**Core Endpoints:**
```
/api/v1/
├── status                # حالة النظام الكاملة
├── hierarchy/status      # حالة الهرم AI
├── command               # تنفيذ أوامر عبر الهرم
├── wisdom                # حكمة من البعد السابع
├── guardian/status       # حالة طبقة الحماية
│
├── ide/
│   ├── files             # شجرة الملفات
│   ├── files/{id}        # محتوى ملف
│   ├── copilot/suggest   # اقتراحات Copilot
│   └── terminal          # محاكي الطرفية
│
├── erp/
│   ├── dashboard         # لوحة معلومات ERP
│   ├── invoices          # الفواتير
│   ├── inventory         # المخزون
│   └── hr                # الموارد البشرية
│
├── council/
│   ├── message           # رسالة للمجلس
│   ├── discussion        # نقاش جماعي
│   ├── history           # سجل المحادثات
│   └── members           # أعضاء المجلس
│
└── rtx4090/
    ├── health            # حالة الـ RTX 4090
    ├── generate          # توليد نص
    └── training/status   # حالة التدريب
```

### 2.3 AI Hierarchy Layer / طبقة الهرم الذكي

**15-Layer Architecture:**

```
┌─────────────────────────────────────────┐
│  Layer 15: Seventh Dimension           │  ◄── الحكمة والبعد الروحي
│  (الرؤية الاستراتيجية)                   │
├─────────────────────────────────────────┤
│  Layer 14: High Council                │  ◄── قرارات المستوى الأعلى
│  (المجلس الأعلى)                         │
├─────────────────────────────────────────┤
│  Layer 13: Meta Architect              │  ◄── تصميم الأنظمة المعقدة
│  (المهندس المعماري)                      │
├─────────────────────────────────────────┤
│  Layer 12: Meta Team                   │  ◄── التنسيق بين الفرق
│  (فريق الميتا)                           │
├─────────────────────────────────────────┤
│  Layer 11: Domain Experts              │  ◄── الخبراء المتخصصون (11)
│  (الخبراء)                              │
├─────────────────────────────────────────┤
│  Layer 10: Scouts                      │  ◄── استكشاف والاستخبارات
│  (الجواسيس)                             │
├─────────────────────────────────────────┤
│  Layer 9:  Guardian Layer              │  ◄── الحماية والأمان (5 طبقات)
│  (طبقة الحماية)                          │
├─────────────────────────────────────────┤
│  Layer 8:  Compliance Layer            │  ◄── الامتثال واللوائح
│  (طبقة الامتثال)                         │
├─────────────────────────────────────────┤
│  Layer 7:  Execution Team              │  ◄── تنفيذ المهام
│  (فريق التنفيذ)                          │
├─────────────────────────────────────────┤
│  Layer 6:  Shadow Light                │  ◄── المراقبة والتحليل
│  (الظل والنور)                          │
├─────────────────────────────────────────┤
│  Layer 5:  Security Layer              │  ◄── الأمان
│  (الأمان)                               │
├─────────────────────────────────────────┤
│  Layer 4:  President                   │  ◄── المستوى التنفيذي
│  (الرئيس)                               │
├─────────────────────────────────────────┤
│  Layer 3:  Connect Services            │  ◄── ربط الخدمات
│  (ربط الخدمات)                          │
├─────────────────────────────────────────┤
│  Layer 2:  Activation                  │  ◄── التفعيل والتشغيل
│  (التفعيل)                              │
├─────────────────────────────────────────┤
│  Layer 1:  Eternity Layer              │  ◄── النسخ الاحتياطي
│  (طبقة الخلود)                          │
└─────────────────────────────────────────┘
```

**Implementation Files:**
```
hierarchy/
├── __init__.py
├── president.py           # Layer 4: الرئيس
├── high_council.py        # Layer 14: المجلس الأعلى (16 حكيم)
├── meta_architect.py      # Layer 13: المهندس المعماري
├── meta_team.py          # Layer 12: فريق الميتا
├── domain_experts.py      # Layer 11: الخبراء (11 مجال)
├── scouts.py             # Layer 10: الجواسيس
├── guardian_layer.py      # Layer 9: الحماية
├── compliance_layer.py    # Layer 8: الامتثال
├── execution_team.py      # Layer 7: التنفيذ
├── shadow_light.py        # Layer 6: الظل والنور
├── security_layer.py      # Layer 5: الأمان
├── connect_services.py    # Layer 3: ربط الخدمات
├── activate.py           # Layer 2: التفعيل
├── eternity_layer.py      # Layer 1: الخلود
├── seventh_dimension.py   # Layer 15: البعد السابع
└── cosmic_bridge.py       # الربط الكوني
```

### 2.4 Smart Council / مجلس الحكماء

**16 Wise Men / 16 حكيم:**

| # | Name | Role | Specialty | Model |
|---|------|------|-----------|-------|
| 1 | حكيم القرار | Decision Maker | القيادة والقرارات | Transformer Layer 1 |
| 2 | حكيم البصيرة | Visionary | التحليل العميق | Transformer Layer 2 |
| 3 | حكيم المستقبل | Futurist | رؤية المستقبل | Transformer Layer 3 |
| 4 | حكيم الشجاعة | Brave | الشجاعة والحسم | Transformer Layer 4 |
| 5 | حكيم التوازن | Balanced | العدل والتوازن | Transformer Layer 5 |
| 6 | حكيم الضبط | Controller | النظام والضبط | Transformer Layer 6 |
| 7 | حكيم التكيف | Adapter | المرونة والتكيف | Transformer Layer 7 |
| 8 | حكيم الحكمة | Wisdom | الحكمة العميقة | Transformer Layer 8 |
| 9 | حكيم الابتكار | Innovator | الابتكار والإبداع | Transformer Layer 9 |
| 10 | حكيم التنفيذ | Executor | التنفيذ والإنجاز | Transformer Layer 10 |
| 11 | حكيم المخاطر | Risk Manager | إدارة المخاطر | Transformer Layer 11 |
| 12 | حكيم التعلم | Learner | التعلم المستمر | Transformer Layer 12 |
| 13 | حكيم الاستراتيجية | Strategist | الاستراتيجية | Transformer Layer 13 |
| 14 | حكيم التحليل | Analyzer | التحليل المعمق | Transformer Layer 14 |
| 15 | حكيم التواصل | Communicator | التواصل | Transformer Layer 15 |
| 16 | حكيم التطوير | Developer | التطوير والبناء | Transformer Layer 16 |

**Alert Levels / مستويات التنبيه:**
```
🟢 GREEN  - وضع طبيعي، استشارة عادية
🟡 YELLOW - تنبيه خفيف، يتطلب اهتمام
🟠 ORANGE - حالة حرجة، يتطلب تحليل
🔴 RED    - أزمة، يتطلب تدخل فوري
⚫ BLACK  - حالة طوارئ قصوى
```

### 2.5 Training System / نظام التدريب

**RTX 4090 Training Server:**

```
Ubuntu 192.168.68.111:9090
├─ Training Server (Port 9090)
│  ├─ 15 Transformer Models (15 طبقة)
│  ├─ 62,000+ Training Epochs
│  ├─ CUDA Acceleration
│  └─ Continuous Learning
│
├─ Inference Server
│  ├─ Model Loading
│  ├─ Text Generation API
│  └─ Batch Processing
│
└─ Checkpoint Management
   ├─ /mnt/ai/checkpoints/
   ├─ Auto-save every N epochs
   └─ Backup to Windows
```

**Model Architecture:**
```python
TransformerModel(
    vocab_size=100-500 per layer,    # حجم المفردات
    d_model=256,                      # أبعاد النموذج
    nhead=8,                          # عدد رؤوس الانتباه
    num_layers=4,                     # عدد الطبقات
    dim_feedforward=1024              # الطبقات الداخلية
)
```

**Checkpoint Files:**
```
learning_data/checkpoints/
├── layer_0.pt  (~96 MB)   - Layer 1: حكيم القرار
├── layer_1.pt  (~98 MB)   - Layer 2: حكيم البصيرة
├── layer_2.pt  (~97 MB)   - Layer 3: حكيم المستقبل
├── layer_3.pt  (~101 MB)  - Layer 4: حكيم الشجاعة
├── layer_4.pt  (~100 MB)  - Layer 5: حكيم التوازن
├── layer_5.pt  (~98 MB)   - Layer 6: حكيم الضبط
├── layer_6.pt  (~99 MB)   - Layer 7: حكيم التكيف
├── layer_7.pt  (~96 MB)   - Layer 8: حكيم الحكمة
├── layer_8.pt  (~97 MB)   - Layer 9: حكيم الابتكار
├── layer_9.pt  (~98 MB)   - Layer 10: حكيم التنفيذ
├── layer_10.pt (~96 MB)   - Layer 11: حكيم المخاطر
├── layer_11.pt (~97 MB)   - Layer 12: حكيم التعلم
├── layer_12.pt (~99 MB)   - Layer 13: حكيم الاستراتيجية
├── layer_13.pt (~100 MB)  - Layer 14: حكيم التحليل
└── layer_14.pt (~98 MB)   - Layer 15: حكيم التواصل
```

---

## 3. Data Flow / تدفق البيانات

### 3.1 Council Message Flow

```
┌─────────────┐     ┌─────────────┐     ┌─────────────┐     ┌─────────────┐
│   User      │────►│   React     │────►│  FastAPI    │────►│  RTX 4090   │
│  Frontend   │     │   Council   │     │   api.py    │     │  Inference  │
└─────────────┘     └─────────────┘     └─────────────┘     └─────────────┘
                                              │                      │
                                              ▼                      ▼
                                       ┌─────────────┐     ┌─────────────┐
                                       │  Smart      │     │  15 Models  │
                                       │  Council    │◄────│  Loaded     │
                                       │  (Local)    │     │  GPU        │
                                       └─────────────┘     └─────────────┘
                                              │
                                              ▼
                                       ┌─────────────┐
                                       │  Response   │
                                       │  (JSON)     │
                                       └─────────────┘
```

### 3.2 IDE Code Flow

```
User Types Code ──► Monaco Editor ──► Copilot Service ──► AI Suggestions
                                           │
                                           ▼
                                    ┌─────────────┐
                                    │  Code       │
                                    │  Analysis   │
                                    │  (AST)      │
                                    └─────────────┘
```

### 3.3 ERP Data Flow

```
ERP Page ──► ERP Service ──► Database ──► Dashboard
                │
                ▼
          ┌─────────────┐
          │  AI         │
          │  Predictions│
          │  (Invoices, │
          │  Inventory) │
          └─────────────┘
```

---

## 4. Communication Protocols / بروتوكولات الاتصال

### 4.1 Windows ↔ Ubuntu Protocol

**HTTP REST API:**
```http
# Health Check
GET http://192.168.68.111:9090/health
Response: {"status": "healthy", "models": 15, "device": "cuda"}

# Generate Text
POST http://192.168.68.111:9090/generate
Body: {
    "prompt": "ما رأيك في...",
    "model": "layer_0",
    "max_length": 100,
    "temperature": 0.8
}
Response: {
    "generated_text": "...",
    "model_used": "layer_0",
    "inference_time": 0.5
}

# Council Message
POST http://192.168.68.111:9090/council/message
Body: {
    "message": "...",
    "user_id": "president",
    "alert_level": "GREEN"
}
Response: {
    "response": "...",
    "council_member": "حكيم القرار",
    "confidence": 0.92
}
```

### 4.2 Environment Variables

**Windows API:**
```bash
RTX4090_HOST=192.168.68.111
RTX4090_PORT=9090
AI_CORE_HOST=192.168.68.111
AI_CORE_PORT=9090
```

**Ubuntu Server:**
```bash
CHECKPOINT_DIR=/mnt/ai/checkpoints
CUDA_VISIBLE_DEVICES=0
FLASK_PORT=9090
```

---

## 5. Security Architecture / بنية الأمان

### 5.1 Guardian Layer (5 Sub-layers)

```
┌─────────────────────────────────────┐
│  Guardian Layer 1: Validation       │ ◄── التحقق من المدخلات
│  التحقق                              │
├─────────────────────────────────────┤
│  Guardian Layer 2: Authorization    │ ◄── التحقق من الصلاحيات
│  التفويض                             │
├─────────────────────────────────────┤
│  Guardian Layer 3: Rate Limiting    │ ◄── تقييد المعدل
│  تقييد الطلبات                       │
├─────────────────────────────────────┤
│  Guardian Layer 4: Encryption       │ ◄── التشفير
│  التشفير                             │
├─────────────────────────────────────┤
│  Guardian Layer 5: Audit Logging    │ ◄── تسجيل الأحداث
│  تسجيل المراجعة                      │
└─────────────────────────────────────┘
```

### 5.2 Compliance Layer

- **Data Privacy:** حماية البيانات
- **Audit Trail:** سجل مراجعة كامل
- **Regulatory Compliance:** الامتثال للوائح
- **Ethical AI:** ذكاء اصطناعي أخلاقي

---

## 6. Storage Architecture / بنية التخزين

### 6.1 File Structure

```
bi-ide-v8/
├── data/                    # بيانات التطبيق
│   ├── erp/                # بيانات ERP
│   ├── ide/                # ملفات IDE
│   └── users/              # بيانات المستخدمين
│
├── learning_data/           # بيانات التعلم
│   ├── checkpoints/        # نقاط الحفظ
│   ├── training_logs/      # سجلات التدريب
│   └── datasets/           # مجموعات البيانات
│
├── hierarchy/               # الهرم الذكي
├── ide/                     # خدمة IDE
├── erp/                     # خدمة ERP
├── ui/                      # واجهة المستخدم
│   └── src/
│       ├── pages/          # الصفحات
│       └── components/     # المكونات
│
├── rtx4090_machine/         # تكوين RTX 4090
├── docs/                    # التوثيق
└── logs/                    # السجلات
```

### 6.2 Data Types

**Checkpoints (~1.5 GB total):**
- Format: PyTorch .pt files
- Size: ~96-101 MB per layer
- Location: Ubuntu /mnt/ai/checkpoints/
- Backup: Windows learning_data/checkpoints/

**User Data:**
- Format: JSON
- Location: data/users/
- Encryption: AES-256

**ERP Data:**
- Format: JSON/SQLite
- Location: data/erp/
- Backup: Daily

---

## 7. Performance Considerations / اعتبارات الأداء

### 7.1 Optimization Strategies

**GPU Acceleration:**
- CUDA for model training/inference
- Batch processing for multiple requests
- Model quantization for faster inference

**Caching:**
- Redis/Memcached for frequent queries
- Browser caching for static assets
- CDN for external libraries

**Async Processing:**
- AsyncIO for API endpoints
- Background tasks for heavy processing
- WebSockets for real-time updates

### 7.2 Bottlenecks & Solutions

| Bottleneck | Solution |
|------------|----------|
| Model Loading Time | Pre-load models, keep in memory |
| Network Latency | Compression, connection pooling |
| GPU Memory | Model sharding, gradient checkpointing |
| Disk I/O | SSD, async writes |

---

## 8. Scaling Strategy / استراتيجية التوسع

### 8.1 Horizontal Scaling

```
┌─────────────────┐
│  Load Balancer  │
└────────┬────────┘
         │
    ┌────┴────┐
    ▼         ▼
┌───────┐  ┌───────┐
│ API 1 │  │ API 2 │  ...  │ API N │
└───┬───┘  └───┬───┘       └───┬───┘
    │          │               │
    └────┬─────┴───────┬───────┘
         ▼             ▼
   ┌──────────┐  ┌──────────┐
   │  RTX 1   │  │  RTX 2   │  (GPU Cluster)
   │ 4090     │  │ 4090     │
   └──────────┘  └──────────┘
```

### 8.2 Future Architecture

- **Microservices:** تقسيم API إلى خدمات صغيرة
- **Kubernetes:** إدارة الحاويات والتوسع
- **Distributed Training:** تدريب موزع عدة GPUs
- **Edge Inference:** inference على الأجهزة الطرفية

---

## 9. Monitoring & Logging / المراقبة والتسجيل

### 9.1 Metrics

**System Metrics:**
- CPU/Memory usage
- GPU utilization (CUDA)
- Network I/O
- Disk usage

**Application Metrics:**
- Request latency
- Error rates
- Model inference time
- Training progress

**Business Metrics:**
- Active users
- Council interactions
- IDE usage
- ERP transactions

### 9.2 Logging Levels

```
DEBUG   - تفاصيل التطوير
INFO    - معلومات عامة
WARNING - تحذيرات
ERROR   - أخطاء
CRITICAL - أخطاء حرجة
```

---

## 10. Deployment / النشر

### 10.1 Development Environment

```bash
# Windows
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
python -m uvicorn api.app:app --host 0.0.0.0 --port 8000

# Ubuntu
cd ~/bi-ide-v8
source venv/bin/activate
python rtx4090_inference_server.py
```

### 10.2 Production Deployment

```bash
# Using Docker (Future)
docker-compose up -d

# Using Systemd
sudo systemctl start bi-ide-api
sudo systemctl start bi-ide-inference
```

---

## Appendix A: Glossary / قاموس المصطلحات

| Term | Description |
|------|-------------|
| Transformer | بنية الشبكة العصبية للـ NLP |
| Checkpoint | نقطة حفظ النموذج |
| Epoch | دورة تدريب كاملة |
| Inference | استخدام النموذج للتنبؤ |
| CORS | Cross-Origin Resource Sharing |
| ASGI | Asynchronous Server Gateway Interface |

---

## Appendix B: Decision Log / سجل القرارات

| Date | Decision | Rationale |
|------|----------|-----------|
| 2026-02-20 | Port 9090 instead of 8080 | Avoid conflict with training |
| 2026-02-20 | strict=False for model loading | Architecture mismatch in checkpoints |
| 2026-02-20 | Character-level tokenizer | Original tokenizer not available |

---

*Document Version: 1.0*
*Last Updated: 2026-02-20*
*Author: BI-IDE Development Team*
