# 🔗 ربط BI IDE مع RTX 4090

## الهيكل
```
[الحاسبة الأولى - أنت]      [الحاسبة الثانية - 4090]
     API Server  ←────────→  Training Server
     Port 8000              Port 8080
     (UI + IDE + ERP)       (AI Training)
```

---

## 🚀 الخطوات

### على الحاسبة الثانية (192.168.68.125)

```bash
# 1. انتقل لمجلد المشروع
cd ~/bi-ide-v8

# 1.5 اجعل سكربت Ubuntu قابل للتنفيذ (مرة واحدة)
chmod +x start_training_ubuntu.sh

# 2. تشغيل خادم التدريب (ينشئ venv ويثبت المتطلبات تلقائياً)
./start_training_ubuntu.sh

# بديل يدوي (إذا رغبت)
# pip install fastapi uvicorn torch transformers numpy
# python rtx4090_server.py

```

راح يظهر:
```
🚀 RTX 4090 Training Server
📡 يستقبل الاتصالات من الابتوب القديم
🔗 http://0.0.0.0:8080
```

**اتأكد من الجدار الناري:**
```bash
# Ubuntu
sudo ufw allow 8080/tcp

# أو iptables
sudo iptables -I INPUT -p tcp --dport 8080 -j ACCEPT
```

---

### على الحاسبة الأولى (هنا)

**الطريقة 1: ملف start_with_rtx4090.bat (جاهز)**
```
انقر مزدوجاً على: start_with_rtx4090.bat
```

**الطريقة 2: PowerShell**
```powershell
$env:AI_CORE_HOST="192.168.68.125"
$env:AI_CORE_PORT="8080"
python -m uvicorn api.app:app --host 0.0.0.0 --port 8000
```

**الطريقة 3: CMD**
```cmd
set AI_CORE_HOST=192.168.68.125
set AI_CORE_PORT=8080
python -m uvicorn api.app:app --host 0.0.0.0 --port 8000
```

---

## ✅ التحقق من الاتصال

على الحاسبة الأولى:
```powershell
# تجربة الاتصال
Invoke-RestMethod -Uri "http://192.168.68.125:8080/status"
```

إذا ظهر JSON ← الاتصال ناجح ✅

---

## 📊 ما يحصل الآن

1. **أنت تشتغل** على IDE هنا ← الكود والبيانات
2. **الـ 4090 يتدرب** بشكل مستقل على البيانات
3. **API يرسل** أوامر التدريب للـ 4090
4. **النتائج** ترجع للحاسبة الأولى

---

## 🔧 تغيير IP (إذا تغير)

عدّل الملفات:
- `start_with_rtx4090.bat` ← السطر 13
- `api.py` ← السطر 25 (افتراضي)

أو استخدم متغير البيئة مباشرة.

---

## 🐛 استكشاف الأخطاء

**مشكلة: Connection Refused**
- تأكد أن `rtx4090_server.py` يعمل على الحاسبة الثانية
- تأكد من الجدار الناري: `sudo ufw allow 8080`

**مشكلة: No route to host**
- تأكد أن كلا الحاسبتين على نفس الشبكة
- جرب ping: `ping 192.168.68.125`

**مشكلة: CORS Error**
- `rtx4090_server.py` يسمح بـ `allow_origins=["*"]`
- إذا UI ما يشتغل، حدد IP الحاسبة الأولى بدل "*"
